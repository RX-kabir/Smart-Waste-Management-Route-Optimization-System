-- 00_extensions.sql
-- Optional extensions. Uncomment what you need.

-- If you want UUID primary keys (not required when using BIGSERIAL):
-- CREATE EXTENSION IF NOT EXISTS pgcrypto;  -- provides gen_random_uuid()

-- If you later use geospatial routing (lat/lng + distance), consider PostGIS:
-- CREATE EXTENSION IF NOT EXISTS postgis;
